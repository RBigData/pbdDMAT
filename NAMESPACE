#useDynLib(pbdDMAT)

import(methods, rlecuyer, pbdMPI, pbdSLAP, pbdBASE)

# All internal functions
exportPattern("^dmat\\.[[:alpha:]]+")

exportMethods(
  "median", "apply", "scale",
  "qr", "qr.Q", "qr.R", "qr.qty", "qr.qy",
  "rcond",
  "lm.fit", "diag", "crossprod", "tcrossprod"
)

export(
  # Arithmetic
  "+", "-", "*", "/", "^", "%%", "%/%", 
  # Algebra
  t, "%*%", crossprod, tcrossprod,
  solve, La.svd, svd, lu, chol, kappa.ddmatrix,
  # Math
  abs, sqrt, log, log2, log10, log1p, round, ceiling, floor,
  # Principal Components
  scale, prcomp, diag,
  # Covariance
  var, cov, sd, scale,
  # Reductions
  sum, mean, prod, rowSums, colSums, rowMeans, colMeans
)
